# Copy this file to .env.local for local overrides (never commit .env.local)

# Backend URL — leave empty in local dev (Vite proxy handles it)
# In Vercel set this to your Render backend URL:
VITE_API_URL=https://chat-app-backend.onrender.com
VITE_WS_URL=https://chat-app-backend.onrender.com
